meta {
  name: Login (2)
  type: http
  seq: 5
}

post {
  url: {{baseurl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{unique_email_user_2}}",
    "password": "{{unique_password_user_2}}"
  }
}

script:post-response {
  bru.setVar("token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
