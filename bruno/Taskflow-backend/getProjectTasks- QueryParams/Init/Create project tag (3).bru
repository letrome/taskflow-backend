meta {
  name: Create project tag (3)
  type: http
  seq: 8
}

post {
  url: {{baseurl}}/projects/{{project_id}}/tags
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Tag3"
  }
}

assert {
  res.status: eq 201
  res.body.name: eq Tag3
  res.body.project: eq {{project_id}}
  res.body.id: matches ^[0-9a-f]{24}$
}

script:post-response {
  bru.setVar("tag_id_3", res.body.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
