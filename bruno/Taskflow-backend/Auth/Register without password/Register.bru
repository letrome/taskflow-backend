meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseurl}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{unique_email}}",
    "first_name": "endt",
    "last_name": "oend"
  }
}

assert {
  res.status: eq 400
  res.body: isJson
  res.body.status: eq error
  res.body.message: eq Password is required
}

script:pre-request {
  const uuid = require("uuid").v4()
  bru.setVar("wrong_email", `${uuid}@example.com`);
}

settings {
  encodeUrl: true
  timeout: 0
}
