meta {
  name: Health (unauthorized)
  type: http
  seq: 4
}

get {
  url: {{baseurl}}/health
  body: none
  auth: inherit
}

assert {
  res.status: eq 401
  res.body: isJson
  res.body.error: eq Unauthorized
}

settings {
  encodeUrl: true
  timeout: 0
}
