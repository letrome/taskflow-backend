meta {
  name: Me
  type: http
  seq: 1
}

get {
  url: {{baseurl}}/me
  body: none
  auth: bearer
}

auth:bearer {
  token: azertyuiop
}

assert {
  res.status: eq 401
  res.body.status: eq error
  res.body.message: eq Unauthorized
}

settings {
  encodeUrl: true
  timeout: 0
}
