meta {
  name: Login (with user manager)
  type: http
  seq: 4
}

post {
  url: {{baseurl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{unique_email_user_manager}}",
    "password": "{{unique_password_user_manager}}"
  }
}

script:post-response {
  bru.setVar("token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
