meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseurl}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "",
    "password": "{{unique_password}}",
    "first_name": "endt",
    "last_name": "oend"
  }
}

assert {
  res.status: eq 400
  res.body: isJson
  res.body.status: eq error
  res.body.message: eq Email is required
}

script:pre-request {
  const uuid = require("uuid").v4()
  bru.setVar("wrong_password", `${uuid}`);
}

settings {
  encodeUrl: true
  timeout: 0
}
