meta {
  name: Metrics
  type: http
  seq: 3
}

get {
  url: {{baseurl}}/metrics
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isString
}

settings {
  encodeUrl: true
  timeout: 0
}
