meta {
  name: Delete me (2)
  type: http
  seq: 5
}

delete {
  url: {{baseurl}}/users/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 404
  res.body.status: eq error
  res.body.message: eq User not found
}

settings {
  encodeUrl: true
  timeout: 0
}
