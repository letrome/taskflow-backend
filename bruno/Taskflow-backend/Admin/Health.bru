meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{baseurl}}/health
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: isJson
  res.body.status: eq OK
}

settings {
  encodeUrl: true
  timeout: 0
}
